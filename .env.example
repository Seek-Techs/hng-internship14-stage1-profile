# Local development — SQLite (no setup needed)
DATABASE_URL=sqlite:///./profiles.db

# Uncomment and fill in when testing with a local PostgreSQL instance
# DATABASE_URL=postgresql://username:password@localhost:5432/profiles_db
